# Postgres. En local : docker compose up -d (voir docker-compose.yml).
# En production : la chaîne fournie par Neon via le Marketplace Vercel.
DATABASE_URL=postgres://bruno:bruno@localhost:5433/bruno
